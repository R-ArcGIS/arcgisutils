% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-envelope.R
\name{as_bbox}
\alias{as_bbox}
\title{Coerce to a bounding box}
\usage{
as_bbox(x, crs = NULL, error_call = rlang::caller_env())
}
\arguments{
\item{x}{An extent. See details.}

\item{crs}{Coordinate reference system to return. Anything \code{\link[wk:wk_crs]{wk::wk_crs()}}
accepts. Used as the CRS when \code{x} carries none.}

\item{error_call}{the caller environment to be used when propagating errors.}
}
\value{
A \code{wk_rct}.
}
\description{
Converts an extent to a \code{\link[wk:rct]{wk::rct()}}, optionally in a different CRS.
}
\details{
Accepts a \code{wk_rct}, an \code{sf} \code{bbox}, a length four numeric
\code{c(xmin, ymin, xmax, ymax)}, or anything \code{\link[wk:wk_bbox]{wk::wk_bbox()}} understands, such as
an \code{sf} or \code{sfc} object. \code{wk} carries a CRS but does not reproject, so when
\code{crs} differs from the extent's own the transform goes through \code{sf}.
}
\examples{
as_bbox(c(-104, 35.6, -94.32, 41), crs = 4326)
}
